# Backend API base URL
# Default for local development with Docker backend on localhost:8080
REACT_APP_API_BASE_URL=http://localhost:8080/api/v1

# Optional: disable host check when using ngrok or custom domain in development
DANGEROUSLY_DISABLE_HOST_CHECK=true
